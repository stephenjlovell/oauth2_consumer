- flash.each do |key, msg|
  .alert.fade.in.alert-dismissible{class: "alert-#{bootstrap_class_for(key)}", role: "alert"}
    %button.close{"aria-label" => "Close", "data-dismiss" => "alert", :type => "button"}
      %span{"aria-hidden" => "true"} &times;
    = msg