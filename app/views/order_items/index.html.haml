.table-responsive
  %table.table.table-hover
    %thead
      %tr
        %th Name
        %th Quantity
        %th Price
        %th Subtotal
        %th{:colspan => "3"}
    %tbody
      - @order_items.each do |item|
        %tr
          %td= link_to item.product.name, item.product
          %td= item.quantity
          %td= number_to_currency(item.product.price, precision: 2)
          %td= number_to_currency(item.product.price*item.quantity, precision: 2)